import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    criteriaContainer: {
        width: '90%',
        marginTop: 8,
        marginBottom: 8,
        alignSelf: 'center',
    },
    criterionRow: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 4,
    },
    criterionIcon: {
        marginRight: 8,
    },
    criterionText: {
        fontSize: 12,
    },
});